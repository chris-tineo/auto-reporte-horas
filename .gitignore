.env
auth/*.json
logs/*.log
__pycache__/
*.pyc
.venv/

# Helpers e inspección de un solo uso (no parte del bot)
tools_*.py
inspect_dump.txt
*.png

# Resumen semanal de tareas (datos operativos, puede ser sensible)
week_note.txt

# Capturas de red (pueden contener tokens de auth)
*_capture.txt
syneos_capture.txt
syneos_token.txt
syneos_timecard.json

# PDFs/exports generados (boletas, timecards) e imágenes del correo
*.pdf
export/
img/
